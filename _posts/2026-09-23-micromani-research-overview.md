---
title: "MicroMani 科研记录总览：研究主线、问题状态与阶段索引"
date: "2026-09-23"
description: "按机械控制、遥操作链路、数据采集、数据契约和模型训练五条主线，整理 MicroMani 阶段科研记录的时间线、当前状态、证据边界和后续任务。"
category: 笔记
tags: [科研记录, MicroMani, 机器人, 具身智能, 数据采集, ACT, 运动控制]
---

MicroMani 的记录最初按时间推进：先解决采集稳定性，再整理数据和训练，随后进入工作原点、机械寻零、质量报告和控制安全。问题逐渐互相交叉后，只按“科研记录（一）（二）（三）……”继续往后编号，已经不足以回答更重要的问题：**某个具体问题现在查到哪里、证据是什么、是否真的验收通过。**

因此这篇作为长期入口：原有时间线文章继续保留，重要问题再按主题归类，并统一标记状态。以后新增实验可以继续写阶段记录，但结论必须能回到这里找到自己的主线、证据和验收边界。

## 一、状态标签

| 标签 | 含义 |
| --- | --- |
| 已定位 | 已有代码、日志或实验把问题缩小到明确机制，但不代表修改已验收 |
| 已修改待验收 | 已有针对性代码修改，尚未完成对应实机或完整回归 |
| 离线验收通过 | 自动化测试、离线数据或仿真验证通过，仍不能替代真实设备验证 |
| 真机验收通过 | 在明确设备、流程和边界下完成重复实机验证 |
| 临时方案 | 当前可绕过问题，但不能作为最终设计 |
| 未解决 | 已观察到问题，但根因或可靠修复仍未闭环 |
| 已否定假设 | 经过对照后，不再把某个早期猜测当作主要解释 |

“代码已经改了”“测试绿了”“现场这一次正常”都不直接等价于“问题已解决”。

## 二、科研记录时间线

| 日期 | 记录 | 主要阶段 |
| --- | --- | --- |
| 2026-09-20 | [阶段一：采集稳定性、数据修复与 ACT 旧数据诊断](/notes/2026/09/20/micromani-research-log/) | 三路相机、录制卡顿、数据修复、早期 ACT 基线与固定样本诊断 |
| 2026-09-21 | [阶段二：统一工作原点、规范示教与 standard10 基线](/notes/2026/09/21/micromani-standardized-data-and-training/) | 工作原点、夹爪语义、A→B 采集协议、standard10、单侧 ACT |
| 2026-09-22 | [阶段三：从回零绕圈到逐轴机械参考点](/notes/2026/09/22/micromani-homing-reference/) | 机械参考点、寻零、限位参考、返回参考点与现场安全边界 |
| 2026-09-22 | [阶段四：采集质量、参与设备与控制安全](/notes/2026/09/22/micromani-quality-and-stability/) | participation、夹爪 stale、迟帧、质量报告、控制租约 |
| 2026-09-23 | [专题复盘：机械寻零完成语义与原点误写](/notes/2026/09/23/micromani-homing-completion-semantics/) | 旧寻零异步完成语义、参考点误写机制、当前修复与 X/Z 剩余问题 |

第一篇公开标题没有显式“（一）”，后续文章使用了编号。为了避免为统一编号修改已有 URL，这里按内容阶段统一称呼，不再强求所有文章标题形式完全一致。

## 三、五条研究主线

### A. 机械系统与运动控制

| 问题 | 当前状态 | 当前结论 |
| --- | --- | --- |
| 工作原点作为统一任务起点 | 已定位 / 已投入采集 | 工作原点承担任务复位语义，应与机械寻零、机械参考点分开 |
| Roll 等旋转轴回零绕圈 | 已定位部分机制 | 日常返回已知位置不应再次机械寻零；异常大旋转需要独立拒绝条件 |
| 寻零过程中错误保存中间 pulse | 已修改待验收 | 旧版把 `dmc_home_move()` 启动成功与寻零完成混淆；修复需要等待 home result、done 和新鲜静止反馈 |
| X/Z 等轴找不到 HOME | 未解决 | 已知会持续运动至端部/超时；仍需核对 HOME 输入、方向、限位逻辑和物理轴映射 |
| 限位端作为机械参考 | 临时/受约束方案 | 只有明确停止原因、输入位、稳定时间和安全条件满足时才允许记录，并必须保留来源 |

### B. 遥操作与控制链路

| 问题 | 当前状态 | 当前结论 |
| --- | --- | --- |
| Omega.7 操作者侧与硬件侧映射 | 已定位 | participation、数据契约和日志必须使用一致的侧别语义 |
| DDS 命令应答并发 | 离线核对通过 | reply 按 `request_id` 关联，并有互斥与条件变量；未发现 lease 请求抢走普通命令 reply 的证据 |
| backend 维护 HAL lease | 已实现但待安全补强 | 能降低 UI 调度抖动导致的误断控，但不能只用 TCP/WebSocket“仍连接”代表操作者界面仍活着 |
| browser liveness | 未完整闭环 | 需要独立 heartbeat；浏览器失活后 backend 应停止替它续 HAL lease |

### C. 数据采集与系统稳定性

| 问题 | 当前状态 | 当前结论 |
| --- | --- | --- |
| 三路相机 30 FPS 与设备绑定 | 真机阶段验证通过 | 已有 120 秒和后续录制记录支持三路稳定采集，但不能外推到所有长期负载 |
| 预览正常但保存视频周期卡顿 | 已定位 / 阶段性解决 | 主要证据指向全代 GC 暂停、编码与图像统计积压；进程隔离和队列保护后明显改善 |
| episode 结束、丢弃、复位竞态 | 已定位 / 有修复 | 收尾顺序属于数据正确性和控制安全的一部分 |
| 夹爪 stale、迟帧和质量 warning | 未完全闭环 | 要区分反馈失效与反馈时间偏移，不能一律判废或一律忽略 |
| participation 驱动质量检查 | 已修改待完整回归 | 应优先使用显式参与侧；运动幅度推断只适合作为旧数据回退策略 |

### D. 数据集与数据契约

| 问题 | 当前状态 | 当前结论 |
| --- | --- | --- |
| 图像统计溢出 | 已修复并重算副本 | 统计错误不等价于视频损坏；已有数据通过重新解码重算，不重编码原视频 |
| 末帧动作异常 | 已定位 | 旧数据保留原值，不凭邻帧猜测修改；训练侧显式排除异常 episode/窗口 |
| 删除/合并后的 LeRobot 索引一致性 | 已定位并有重建方案 | 文件、episode 元数据、统计和映射必须一起维护 |
| standard10 规范筛选 | 离线验收通过 | 作为可追溯诊断/训练基线使用，不把“任务成功”直接等同于“数据完全无异常” |

### E. 模型训练与评估

| 问题 | 当前状态 | 当前结论 |
| --- | --- | --- |
| 早期低分辨率/轻量试训 | 流程验证完成 | 只证明训练链路可运行，不能作为微小零件视觉性能结论 |
| 640×480 三视角 ACT 基线 | 离线实验完成 | 已建立可重载训练与固定评估路径，但部分基线仍弱于“保持当前 state”对照 |
| 30 步预测偏平 | 已缩小问题范围 | 延长训练、单独关闭 VAE 均不能充分解释；固定 8 窗说明网络具备拟合时间变化的能力 |
| 单侧 7 维训练 | 离线对照有改善 | 相对 14 维基线验证误差下降，但仍不能据此宣称自主抓放可靠 |
| 自主抓放实机 rollout | 尚未完成 | 当前模型结论仍以离线验证为主 |

## 四、当前问题树

```text
MicroMani
├─ 机械控制
│  ├─ 寻零完成语义：已定位，已修改待真机验收
│  ├─ X/Z HOME 信号/方向/映射：未解决
│  └─ 工作原点与机械参考点：概念和入口已拆分
├─ 控制链路
│  ├─ DDS request/reply：已核对
│  └─ browser liveness + HAL lease：待补强
├─ 数据采集
│  ├─ 三路相机与历史卡顿：阶段性解决
│  ├─ 夹爪 stale / 迟帧：继续量化
│  └─ participation 质量规则：待完善
├─ 数据契约
│  ├─ 历史数据修复与索引：已有可追溯方案
│  └─ standard10：规范基线
└─ 模型
   ├─ ACT 离线链路：已建立
   ├─ Z / 夹爪误差：仍是主要误差来源
   └─ 自主抓放实机验证：尚未完成
```

## 五、近期优先级

当前不适合同时推进所有分支。按安全性和对后续实验的影响，优先顺序是：

1. **机械寻零闭环。** 验证失败、超时、急停后不会覆盖 `homeReference`，再查 X/Z HOME 输入、方向和轴映射。
2. **控制租约安全语义。** 保留 backend 高频续租，同时建立独立于 TCP 连接状态的 browser liveness。
3. **前端回归清理。** 当前 typecheck 通过，但仍有测试失败和 lint error；新一轮长时间采集前应清理。
4. **质量报告与 participation。** 优先使用显式参与侧，减少运动幅度推断造成的误判。
5. **重新补采规范数据。** 机械参考、控制安全和质量判据稳定后，再扩大示教量。
6. **继续模型实验。** 新数据稳定后再比较 ACT 或后续模型，避免用调参掩盖采集和坐标问题。

前四项会直接改变训练数据的坐标基准、有效帧和动作标签。如果底层语义还在变化，继续堆训练轮次很难得到可解释的结论。

## 六、以后怎样写科研记录

后续文章继续保留时间线，但至少回答四件事：

- 这次解决的是哪条主线；
- 证据来自现场观察、日志、代码、离线测试还是真机重复实验；
- 状态从什么变成了什么；
- 哪些结论仍然不能说。

历史文章不为了后见之明而改写当时的判断。后续若推翻早期假设，应在新记录中明确写“已否定假设”，并链接回原记录。

## 七、当前整理结论

现在的 MicroMani 记录已经不只是训练日志，而是从机械运动、遥操作安全、数据采集、数据契约一路延伸到模仿学习的完整工程研究链。

从这篇开始，**时间线继续保留，但问题状态成为第一索引。** 无论后面继续调 X/Z 寻零、查控制租约、补示教数据还是重新训练模型，都先确认依赖是否已经验收、哪些结论仍只是候选解释、下一次实验究竟要关闭哪个不确定性。

本总览只整理已有记录和当前已知状态，不替代各篇文章中的原始证据，也不把本地未提交代码或未完成实机验证写成最终结果。
