# YoungShip 博客协作规则

## 项目与范围

- 这是 `YoungShip/youngship.github.io` 的个人主页和记录站；GitHub Pages 从 `main` 根目录构建，公开地址为 `https://youngship.github.io/`。
- `_posts/` 是已发布文章，`assets/uploads/` 是文章附件，`admin/` 是网页写作后台。以 `_config.yml` 和 `admin/config.yml` 为实际格式依据。
- 写文章不意味着获准修改首页、主题、部署方式、后台权限或其他文章。

## 默认只写草稿

- “写文章”“整理笔记”“修改草稿”不授权 commit、push、创建公开 PR 或点击后台“保存”。只有用户明确要求发布具体稿件时才执行公开提交。
- 草稿放在 Git 忽略的 `.local-drafts/`，先确认没有被跟踪；不能保证忽略时放到仓库外。不要将私人草稿提交到公开仓库的任何分支；`_drafts/` 也不是保密空间。
- 已有用户改动与其他 Agent 的工作必须保留。只暂存本次获准的稿件和附件；不要 `git add .`、强推、覆盖冲突或夹带已有暂存文件。
- 内容依据用户素材；保留口吻，不编造亲历、实验结果、数据和来源。区分推测，发布前检查私人信息与密钥。

## 文章约定

- 文件名：`_posts/YYYY-MM-DD-short-slug.md`；UTF-8，YAML front matter 包含 `title`、`date`、`description`、`category`，可选 `tags` 字符串列表；正文为 Markdown。
- 新文章必须填真实有效的 date，与文件名一致；按 `Asia/Shanghai` 处理日期。旧文章若只有文件名日期，编辑时保留原日期并补齐，不改成当天。
- 分类：`笔记`、`踩坑`、`随想`、`生活记录`。新 slug 建议小写英文和连字符；不要未经要求改已有文章路径。
- 日期在未来的文章不会保证自动按时上线，不能把它当作已配置的定时发布功能。
- 站内图片用 `/assets/uploads/...`；不提交未经允许公开的图片或附件。含 Liquid 语法的示例需考虑 Jekyll 处理并检查渲染。

## 授权与发布核验

- 使用环境已有的 Git/连接器认证，不读取或复制浏览器存储、系统凭据或令牌，不将秘密写入文件或日志。缺授权就停在草稿阶段，请用户通过平台正规登录授权。
- Skill 和本规则只是工作约定，不授予权限，也不是强制安全隔离；公开仓库规则不应含私人路径或秘密。
- 发布前审查文章与暂存 diff、有效 YAML/日期、链接、隐私及远端状态；只提交批准的内容。推送后检查对应提交的 Pages 构建和最终文章页面。失败或无法验证时准确说明，不声称已上线。
- `admin/` 中“保存”会提交并公开内容，不可作为私人草稿保存。
- 博客写作任务优先使用仓库内 `.agents/skills/blog-writing/SKILL.md`（`$blog-writing`）。若环境未自动发现技能，直接读取该文件；若同时存在同名个人技能，以本仓库内的流程为准。不需要复制任何令牌。
- 多台电脑开始写作前先检查远端更新；只在工作区干净且能快进时同步。有未提交改动、分叉或冲突时保留原内容，不自动覆盖。未发布的 `.local-drafts/` 不会同步到其他电脑。
